<Window x:Class="DataLens.WorkspaceEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Workspace" Height="520" Width="520" MinHeight="400" MinWidth="460"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="#F5F5F5">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="0">
        <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Workspace name" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <TextBox x:Name="TxtName" Grid.Row="1" Margin="0,0,0,12" Padding="8,6"/>

        <TextBlock Grid.Row="2" Text="Server (host)" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <TextBox x:Name="TxtHost" Grid.Row="3" Margin="0,0,0,12" Padding="8,6" Text="localhost"/>

        <TextBlock Grid.Row="4" Text="Port" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <TextBox x:Name="TxtPort" Grid.Row="5" Width="80" HorizontalAlignment="Left" Padding="8,6" Margin="0,0,0,12" Text="3306"/>

        <TextBlock Grid.Row="6" Text="Database" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <TextBox x:Name="TxtDatabase" Grid.Row="7" Margin="0,0,0,12" Padding="8,6"/>

        <TextBlock Grid.Row="8" Text="User name" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <TextBox x:Name="TxtUserName" Grid.Row="9" Margin="0,0,0,12" Padding="8,6"/>

        <TextBlock Grid.Row="10" Text="Password" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <PasswordBox x:Name="TxtPassword" Grid.Row="11" Margin="0,0,0,20" Padding="8,6"/>

        <StackPanel Grid.Row="12" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button x:Name="BtnSave" Content="Save" Width="90" Height="32" Margin="0,0,8,0"
                    Click="BtnSave_OnClick" IsDefault="True"/>
            <Button Content="Cancel" Width="90" Height="32" IsCancel="True"/>
        </StackPanel>
    </Grid>
    </ScrollViewer>
</Window>

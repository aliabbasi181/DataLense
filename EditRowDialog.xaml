<Window x:Class="DataLens.EditRowDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit row — generate UPDATE query" Height="560" Width="640"
        MinHeight="440" MinWidth="520"
        WindowStartupLocation="CenterOwner"
        Background="#FAFAFA">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Edit values and click Generate to build an UPDATE query (WHERE uses primary key only)." 
                   TextWrapping="Wrap" Margin="0,0,0,8" Foreground="#555"/>
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" MaxHeight="280" Margin="0,0,0,8">
            <ItemsControl x:Name="EditItemsControl">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="0,2">
                            <TextBlock Text="{Binding ColumnName}" Width="140" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <TextBox Text="{Binding NewValue, UpdateSourceTrigger=PropertyChanged}" Width="240"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,6">
            <Button x:Name="BtnGenerate" Content="Generate UPDATE query" Width="160" Height="28" Margin="0,0,8,0" Click="BtnGenerate_OnClick"/>
            <Button x:Name="BtnCopy" Content="Copy" Width="70" Height="28" Click="BtnCopy_OnClick"/>
        </StackPanel>
        <Grid Grid.Row="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="Generated UPDATE query:" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="TxtGeneratedUpdate" Grid.Column="0" IsReadOnly="True" FontFamily="Consolas" FontSize="12"
                         Padding="8,6" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                         BorderBrush="#CCC" BorderThickness="1" Background="#FFF" MinHeight="100" Margin="0,0,6,0"/>
                <Button x:Name="BtnClose" Grid.Column="1" Content="Close" Width="70" Height="28" Click="BtnClose_OnClick" VerticalAlignment="Top"/>
            </Grid>
        </Grid>
    </Grid>
</Window>
